<h2>Users</h2>
<div class="container">
  <div class="row g-2 users">
      {{#each users}}
        <div class="card mx-0 mx-sm-1 col-12 col-sm-5 user">
            <h5 class="mb-1">
              <a href="/users/{{id}}" class="text-primary">{{username}}</a>
            </h5>
            <p>
              Member since {{format_date created_at}}
            </p>
        </div>
      {{/each}}
  </div>
</div>